<Window x:Class="CvarcLogger.App.Views.RenameModeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Rename Tab" Icon="/Assets/AppIcon.ico" Height="180" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False" Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Tab name" FontSize="12" FontWeight="Bold" Foreground="#555555" Margin="0,0,0,5"/>

        <TextBox x:Name="LabelTextBox" Grid.Row="1" Background="White" BorderBrush="#555555" BorderThickness="1"
                 Padding="10" FontSize="14" KeyDown="LabelTextBox_KeyDown"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel" Click="Cancel_Click"
                    Background="#7f8c8d" Foreground="White" BorderThickness="0"
                    Padding="16,8" FontSize="13" FontWeight="SemiBold" Margin="0,0,10,0"/>
            <Button Content="OK" Click="Ok_Click"
                    Background="#2E7D32" Foreground="White" BorderThickness="0"
                    Padding="16,8" FontSize="13" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
